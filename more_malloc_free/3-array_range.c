#include "main.h"

/**
 *
 *
 *
 *
 */

int *array_range(int min, int max)
{
	if (size)
	{
		str = malloc((sizeof(char) * size));

		if (str == NULL)
		{
			return (NULL);
		}
		for (i = 0; i < ; i++)
		{
			[i] = ;
		}
	}
	return ();
}
